diff --git a/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/gamma/tabs/TabsHost.kt b/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/gamma/tabs/TabsHost.kt
index 73ac57d..6b772dd 100644
--- a/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/gamma/tabs/TabsHost.kt
+++ b/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/gamma/tabs/TabsHost.kt
@@ -3,11 +3,15 @@ package com.swmansion.rnscreens.gamma.tabs
 import android.content.res.Configuration
 import android.view.Choreographer
 import android.view.MenuItem
+import android.view.ViewGroup.MarginLayoutParams
 import android.widget.FrameLayout
 import android.widget.LinearLayout
 import androidx.appcompat.view.ContextThemeWrapper
+import androidx.core.view.ViewCompat
+import androidx.core.view.WindowInsetsCompat
 import androidx.fragment.app.FragmentManager
 import com.facebook.react.modules.core.ReactChoreographer
+import com.facebook.react.uimanager.PixelUtil
 import com.facebook.react.uimanager.ThemedReactContext
 import com.google.android.material.bottomnavigation.BottomNavigationView
 import com.swmansion.rnscreens.BuildConfig
@@ -93,7 +97,43 @@ class TabsHost(
 
     private val bottomNavigationView: BottomNavigationView =
         BottomNavigationView(wrappedContext).apply {
-            layoutParams = LayoutParams(LayoutParams.MATCH_PARENT, LayoutParams.WRAP_CONTENT)
+            // PATCH (Android-only, xem patches/react-native-screens+4.16.0.patch
+            // trong repo app) — thanh tab nổi: lề trái/phải + đáy thay vì dính
+            // sát các cạnh, để khớp bố cục "thanh tab nổi" của app. Lề phải
+            // rộng hơn lề trái để chừa chỗ cho FloatingCartButton — nút tròn
+            // 64dp tự vẽ phía JS (xem components/navigation/floating-cart-button.tsx
+            // và app/(tabs)/_layout.tsx trong repo app), nổi cùng hàng bên
+            // phải thanh tab trên Android. Không ảnh hưởng iOS — file này chỉ
+            // build trên Android.
+            layoutParams =
+                LayoutParams(LayoutParams.MATCH_PARENT, LayoutParams.WRAP_CONTENT).apply {
+                    val left = PixelUtil.toPixelFromDIP(12f).toInt()
+                    // 12 (lề gốc, khớp lề trái) + 64 (bề rộng FloatingCartButton) +
+                    // 12 (khoảng hở giữa thanh tab và nút) = 88dp.
+                    val right = PixelUtil.toPixelFromDIP(88f).toInt()
+                    val bottom = PixelUtil.toPixelFromDIP(10f).toInt()
+                    setMargins(left, 0, right, bottom)
+                }
+            elevation = PixelUtil.toPixelFromDIP(8f)
+            // Thanh nổi nên nó nằm TRÊN vùng cử chỉ chứ không chạm đáy màn.
+            // Material mặc định cộng inset cử chỉ vào PADDING đáy của thanh —
+            // thanh phình lên 104dp và chừa một dải trống dưới nhãn. Đổi chỗ
+            // inset đó sang LỀ đáy: thanh giữ đúng 80dp của Material 3 và vẫn
+            // nằm trên vạch cử chỉ, cùng mốc với FloatingCartButton phía JS
+            // (insets.bottom + 10dp).
+            ViewCompat.setOnApplyWindowInsetsListener(this) { view, insets ->
+                val systemBottom =
+                    insets.getInsets(WindowInsetsCompat.Type.systemBars()).bottom
+                view.setPadding(0, 0, 0, 0)
+                (view.layoutParams as? MarginLayoutParams)?.let {
+                    val gap = PixelUtil.toPixelFromDIP(10f).toInt()
+                    if (it.bottomMargin != systemBottom + gap) {
+                        it.bottomMargin = systemBottom + gap
+                        view.requestLayout()
+                    }
+                }
+                WindowInsetsCompat.CONSUMED
+            }
         }
 
     private val contentView: FrameLayout =
diff --git a/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/gamma/tabs/TabsHostAppearanceApplicator.kt b/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/gamma/tabs/TabsHostAppearanceApplicator.kt
index 1f75d24..915ce00 100644
--- a/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/gamma/tabs/TabsHostAppearanceApplicator.kt
+++ b/node_modules/react-native-screens/android/src/main/java/com/swmansion/rnscreens/gamma/tabs/TabsHostAppearanceApplicator.kt
@@ -2,6 +2,7 @@ package com.swmansion.rnscreens.gamma.tabs
 
 import android.annotation.SuppressLint
 import android.content.res.ColorStateList
+import android.graphics.drawable.GradientDrawable
 import android.util.TypedValue
 import android.view.MenuItem
 import android.view.ViewGroup
@@ -27,10 +28,18 @@ class TabsHostAppearanceApplicator(
 
     fun updateSharedAppearance(tabsHost: TabsHost) {
         bottomNavigationView.isVisible = true
-        bottomNavigationView.setBackgroundColor(
-            tabsHost.tabBarBackgroundColor
-                ?: resolveColorAttr(com.google.android.material.R.attr.colorSurfaceContainer),
-        )
+        // PATCH (Android-only, xem patches/react-native-screens+4.16.0.patch
+        // trong repo app) — nền bo góc cho thanh tab nổi, đi cùng lề trong
+        // TabsHost.kt. Không ảnh hưởng iOS — file này chỉ build trên Android.
+        bottomNavigationView.background =
+            GradientDrawable().apply {
+                shape = GradientDrawable.RECTANGLE
+                cornerRadius = PixelUtil.toPixelFromDIP(40f)
+                setColor(
+                    tabsHost.tabBarBackgroundColor
+                        ?: resolveColorAttr(com.google.android.material.R.attr.colorSurfaceContainer),
+                )
+            }
 
         val states =
             arrayOf(
diff --git a/node_modules/react-native-screens/android/src/main/res/base/anim/rns_no_animation_medium.xml b/node_modules/react-native-screens/android/src/main/res/base/anim/rns_no_animation_medium.xml
index 560eda6..f61d3a0 100644
--- a/node_modules/react-native-screens/android/src/main/res/base/anim/rns_no_animation_medium.xml
+++ b/node_modules/react-native-screens/android/src/main/res/base/anim/rns_no_animation_medium.xml
@@ -2,6 +2,6 @@
 <alpha xmlns:android="http://schemas.android.com/apk/res/android"
     android:fromAlpha="1.0"
     android:toAlpha="1.0"
-    android:duration="@android:integer/config_mediumAnimTime"
+    android:duration="200"
     />  <!--Remember to change duration in the corresponding xml when modifying it-->
 
diff --git a/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_in_from_bottom.xml b/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_in_from_bottom.xml
index 052fe15..f0ed3cf 100644
--- a/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_in_from_bottom.xml
+++ b/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_in_from_bottom.xml
@@ -2,6 +2,7 @@
 <translate xmlns:android="http://schemas.android.com/apk/res/android"
     android:fromYDelta="100%"
     android:toYDelta="0%"
-    android:duration="@android:integer/config_mediumAnimTime" />  <!--we use
+    android:duration="200"
+    android:interpolator="@android:interpolator/accelerate_decelerate" />  <!--we use
         rns_no_animation_medium.xml as the other animation for this transition since we want both of
         them to end at the same time. Remember to change duration in both files when modifying it-->
diff --git a/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_in_from_left.xml b/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_in_from_left.xml
index 939110f..bda848d 100644
--- a/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_in_from_left.xml
+++ b/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_in_from_left.xml
@@ -1,5 +1,6 @@
 <?xml version="1.0" encoding="utf-8"?>
 <translate xmlns:android="http://schemas.android.com/apk/res/android"
-    android:duration="@android:integer/config_mediumAnimTime"
+    android:duration="200"
+    android:interpolator="@android:interpolator/accelerate_decelerate"
     android:fromXDelta="-100%"
     android:toXDelta="0%" />
diff --git a/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_in_from_right.xml b/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_in_from_right.xml
index 428eb9b..3753f2b 100644
--- a/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_in_from_right.xml
+++ b/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_in_from_right.xml
@@ -1,5 +1,6 @@
 <?xml version="1.0" encoding="utf-8"?>
 <translate xmlns:android="http://schemas.android.com/apk/res/android"
-    android:duration="@android:integer/config_mediumAnimTime"
+    android:duration="200"
+    android:interpolator="@android:interpolator/accelerate_decelerate"
     android:fromXDelta="100%"
     android:toXDelta="0%" />
diff --git a/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_out_to_bottom.xml b/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_out_to_bottom.xml
index 6b75d25..dd0326f 100644
--- a/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_out_to_bottom.xml
+++ b/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_out_to_bottom.xml
@@ -2,6 +2,7 @@
 <translate xmlns:android="http://schemas.android.com/apk/res/android"
     android:fromYDelta="0%"
     android:toYDelta="100%"
-    android:duration="@android:integer/config_mediumAnimTime" />  <!--we use
+    android:duration="200"
+    android:interpolator="@android:interpolator/accelerate_decelerate" />  <!--we use
         rns_no_animation_medium.xml as the other animation for this transition since we want both of
         them to end at the same time. Remember to change duration in both files when modifying it-->
diff --git a/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_out_to_left.xml b/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_out_to_left.xml
index 400a202..5282d39 100644
--- a/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_out_to_left.xml
+++ b/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_out_to_left.xml
@@ -1,5 +1,6 @@
 <?xml version="1.0" encoding="utf-8"?>
 <translate xmlns:android="http://schemas.android.com/apk/res/android"
-    android:duration="@android:integer/config_mediumAnimTime"
+    android:duration="200"
+    android:interpolator="@android:interpolator/accelerate_decelerate"
     android:fromXDelta="0%"
     android:toXDelta="-100%"/>
diff --git a/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_out_to_right.xml b/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_out_to_right.xml
index a00332b..f5de8cf 100644
--- a/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_out_to_right.xml
+++ b/node_modules/react-native-screens/android/src/main/res/base/anim/rns_slide_out_to_right.xml
@@ -1,5 +1,6 @@
 <?xml version="1.0" encoding="utf-8"?>
 <translate xmlns:android="http://schemas.android.com/apk/res/android"
-    android:duration="@android:integer/config_mediumAnimTime"
+    android:duration="200"
+    android:interpolator="@android:interpolator/accelerate_decelerate"
     android:fromXDelta="0%"
     android:toXDelta="100%"/>
